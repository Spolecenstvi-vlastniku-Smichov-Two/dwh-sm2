# DWH-SM2-0001: Bootstrap DWH-SM2 epic: identity, StoryPrefix declaration, founding knowledge

> Bootstrap DWH-SM2 epic: identity, StoryPrefix declaration, founding knowledge

**Type:** default
**Epic:** [DWH-SM2](DWH-SM2)
**Status:** in_progress
**Blueprint:** [implementation-story-blueprint](implementation-story-blueprint)

## Goal

Register dwh-sm2 (SM2 building data warehouse) as an ahabase Epic: identity document carrying the overheating-complaint vision, canonical StoryPrefix declaration, and a verified registration.

## Requirements

### R1: Epic identity document with vision

**Acceptance Criterion:** `_grid4d/story/DWH-SM2.md` exists with Overview and Vision describing the evidentiary role of the warehouse (ČSN 730540-2, complaint against chronic overheating); epic registered in ahabase.yaml with clean remote URL and prefix mapping. (Landed on main in 1473e4f via `epic init` + Human-provided vision.)

### R2: Canonical StoryPrefix declaration

**Acceptance Criterion:** The identity document declares `**StoryPrefix:** DWH-SM2-` — the declaration home per the story identity pattern, not only the registry fallback.

### R3: Registration verified

**Acceptance Criterion:** `knowledge-usage doctor` reports no StoryPrefix warning or divergence for dwh-sm2; the epic is discoverable by the CLI.

## Notes

Created: 2026-09-22
Epic bootstrapped with `knowledge-usage epic init dwh-sm2` (EVOLUCEAN-0240 machinery). Vision supplied by Human: the Epic exists to achieve a successful formal complaint about SM2 building overheating — see identity document for full background and external references (blog, wiki, public data explorer).
