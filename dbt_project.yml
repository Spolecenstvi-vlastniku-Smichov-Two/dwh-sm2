name: 'dwh_sm2'

config-version: 2
version: '0.1'

profile: 'sm2'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

models:
  dwh_sm2:
    ventilation:
      +materialized: external
      +format: csv

      fact:
        +location: "./gdrive/fact.csv"

    indoor:
      +materialized: external
      +format: csv

      fact_indoor_temperature:
        +location: "./gdrive/fact_indoor_temperature.csv"

      fact_indoor_humidity:
        +location: "./gdrive/fact_indoor_humidity.csv"
